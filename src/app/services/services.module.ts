import { NgModule } from '@angular/core';

// services to import
import { LogService } from './log/log.service';

@NgModule({
    imports: [
    ],
    declarations: [
    ],
    providers: [
        LogService
    ]
})
export class ServicesModule { }